La ruta POST /reporte recibe información enviada por el usuario en formato JSON y devuelve una respuesta confirmando que el reporte fue recibido.

Esta ruta podría servir en una aplicación comunitaria para que los ciudadanos envíen reportes sobre problemas, alertas o solicitudes.